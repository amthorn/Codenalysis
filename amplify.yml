version: 1
frontend:
  phases:
    build:
      commands: 
        - ls -la
        - cd src/Codenalysis
        - ls -la
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
