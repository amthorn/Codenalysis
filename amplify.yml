version: 1
frontend:
  phases:
    build:
      commands: 
        - ls -la
        - cd src/Codenalysis
        - ls -la
        - npm ci
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
