version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd src
    build:
      commands: 
        - ls -la
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
