version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands: 
        - ls -la
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
